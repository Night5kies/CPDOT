# Single baseline run: all weights at defaults (1.0), 10 trials, 600s per-trial timeout.

n_trials_per_config: 10
timeout_per_trial_s: 300

pkg_result_dir: "/home/night/CPDOT2/src/CPDOT2/formation_planner/traj_result"
output_dir: "/home/night/CPDOT2/src/CPDOT2/formation_planner/traj_result/sweep_outputs"
topo_prm_seed: 42
offset_random_seed_per_config: false
seed_stride_per_config: 1000

param_grid:
  random_seed: [42]
  opti_t: [1.0]
  opti_w_a: [1.0]
  opti_w_omega: [1.0]
  opti_w_penalty0: [10000.0]
  opti_w_formation: [1.0]
  opti_w_topo: [1.0]
  opti_w_sfc: [1.0]
